#!/bin/sh
# After each local commit on main, push to origin. Enable once: npm run setup:hooks
branch=$(git symbolic-ref --short HEAD 2>/dev/null) || exit 0
[ "$branch" = "main" ] || exit 0
git push -u origin "$branch"
