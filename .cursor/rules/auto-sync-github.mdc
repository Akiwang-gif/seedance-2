---
description: 完成改动后自动提交并推送到 GitHub（小改动默认同步）
alwaysApply: true
---

# 同步到 GitHub（默认开启）

在 **seedance-2** 仓库中完成用户请求的代码或内容改动后：

1. **默认执行**：若工作区有未提交的、与本次任务相关的变更，在任务收尾时运行 `git status`，对本次修改的文件 `git add`，用一句英文或中英文简明说明写 `git commit`，再执行 `git push origin main`（当前分支若为 `main`；若用户在其他分支，则 `git push -u origin <当前分支名>`）。
2. **以下情况跳过推送**：用户明确说不要提交、不要推送、仅本地修改、或要求先给 diff/待审；仅有未跟踪的临时文件且无实质改动；推送明显会失败且用户未要求重试（可说明需配置凭据）。
3. **范围**：以「本次会话中为满足请求而改动的文件」为主，不要擅自把无关未暂存文件一并提交。
4. **提交信息**：使用 `type(scope): summary` 或简短祈使句，与仓库现有风格一致。

可选（用户本地一次性启用「每次自己 `git commit` 后自动 `git push`」）：在项目根目录执行 `npm run setup:hooks`（将 `core.hooksPath` 指向 `.githooks`）。
