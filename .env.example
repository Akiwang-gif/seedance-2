# SiliconFlow（视频 API）
SILICONFLOW_API_KEY=your_siliconflow_key

# Neon 数据库
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# NextAuth + Google 登录
# 本地开发：NEXTAUTH_URL=http://localhost:3000（若端口是 3001 则改为 3001）
# 线上部署（Vercel）：NEXTAUTH_URL=https://你的域名（如 https://seedance-2.info，不要结尾斜杠）
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_random_secret_at_least_32_chars
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Google 控制台需添加对应重定向 URI：{NEXTAUTH_URL}/api/auth/callback/google
